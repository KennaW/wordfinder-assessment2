Specs for word instance app

1 -- Check for one letter (a) in a one letter string. (a) return 1 // simple test to check if one string can see another one

2 -- check for one letter (a) in a two letter string. (ab) return 1 // test if function is finding just one instance

3 -- check for one letter (a) in a two letter string. (aa) return 2 // test if function can check for more than one instance

4 -- check for a short string (the) in a long string (the count of monte cristo) return 1 // test if function can check for a multiple character string

5 -- check for a short string (the) in a long string (the cat in the hat) return 2 // test if function can check for multiple instances of multi character